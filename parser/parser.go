package parser

import (
	// "com/parser/ui"
	// "com/parser/ui/components"
	// "fmt"
	// "time"
)

func Parse() {
	// var pb components.ProgressBar
	// pb.SetPercentage(0.0)
	// pb.SetPrefix("Parsing:")
	// pb.SetSuffix("0/0");
	// pb.SetWidth(20)
	//
	// total := 50000
	//
	// updateSuffix := func (parsed, total int) {
	// 	pb.SetSuffix(fmt.Sprintf("%d/%d", parsed, total))
	// }
	//
	// updatePercentage := func (parsed, total int) {
	// 	pb.SetPercentage(float32(parsed) / float32(total) * 100)
	// }
	//
	// ui.Render(pb)
	//
	// for i := 0; i < total; i+= 562 {
	// 	time.Sleep(50 * time.Millisecond)
	// 	updatePercentage(i, total)
	// 	updateSuffix(i, total)
	// 	ui.ReRender(pb)
	// }
	// pb.SetPercentage(100)
	// updateSuffix(50000, 50000)
	// ui.ReRender(pb)
	// fmt.Println()

	// eh := appContext.EventHandler()
	// eh.Publish(events.EventProgressStart, "Tracking progress on parsing")
	// for i := 0.0; i < 100; i += 1.24 {
	// 	eh.Publish(events.EventProgressDraw, fmt.Sprintf("\rLoading: %.2f%%", i))
	// 	time.Sleep(50 * time.Millisecond)
	// }
	// eh.Publish(events.EventProgressDraw, fmt.Sprintf("\rLoading: 100.00%%"))
	// eh.Publish(events.EventProgressComplete, "Tracking complete")
}
